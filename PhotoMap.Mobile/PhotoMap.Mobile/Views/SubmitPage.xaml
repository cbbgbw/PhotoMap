<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PhotoMap.Mobile.Views.SubmitPage">
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Your photo" FontSize="Medium" />
            <Image x:Name="PhotoImage" />
            <Label Text="Name it" FontSize="Medium" />
            <Entry Text="{Binding PhotoMapItem.Text}" d:Placeholder="River nearby" FontSize="Small" />
            <Label Text="Tell more about it" FontSize="Medium" />
            <Editor Text="{Binding PhotoMapItem.Description}" d:Placeholder="My photo pictures river nearby my house" FontSize="Small" Margin="0" />
            <Button Text="Upload this art" x:Name="ArtUploader"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>